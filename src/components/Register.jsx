import React from "react";
import { DataContext } from "../context/DataProvider";

const Register = () => {
    return (
        <>
            <h2>Registraste</h2>
        </>
    );
};

export default Register;
